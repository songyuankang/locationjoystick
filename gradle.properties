org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true

android.useAndroidX=true
android.nonTransitiveRClass=true
android.enableJetifier=false
# Workspace path contains non-ASCII characters (D:\定位) — allow AGP to build in place.
android.overridePathCheck=true
# AGP 9 defaults this to true, dropping testReleaseUnitTest — `make test` (and CI) target
# the release variant specifically since it's what R8/shrinking actually ships.
android.onlyEnableUnitTestForTheTestedBuildType=false

kotlin.code.style=official

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true

# Kover 0.9.9 agent mangles non-ASCII project paths on Windows (D://定位) —
# disable instrumentation; coverage still available via make coverage on CI.
kover.disabled=true
